mixin forgotForm()
  form.form(action="/login/forgot" method="POST")
    h3 I forgot my password
    label(for="email") Email Address
    input(type="email" name="email")
    input.button(type="submit" value="Send  a Reset >")